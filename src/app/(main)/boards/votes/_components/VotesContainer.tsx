import VotesList from "./VotesList";

function VotesContainer() {
  return (
    <section>
      {/* 글쓰기 버튼 */}
      {/* 정렬 버튼 */}
      <VotesList />
    </section>
  );
}

export default VotesContainer;
